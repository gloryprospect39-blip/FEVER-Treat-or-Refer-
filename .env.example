# FeverGate — Next.js environment variables
# Copy to .env.local and fill in. Never commit real keys.

# Optional: override where local data files (encounters log, patient registry)
# are written. Defaults to ./data, falling back to the OS temp dir on
# read-only/serverless filesystems.
# FEVERGATE_DATA_DIR=
